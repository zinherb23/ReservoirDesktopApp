💧 Taiwan Reservoir Telemetry Monitor

A desktop application built with C# and WinForms to monitor reservoir. 

🛠️ Tech Stack & Dependencies  

Language: C#  
Runtime: .NET 8.0  
Framework: WinForms  

📁 Project Structure
```text
├── data/                               # Target runtime directory for local telemetry storage
├── Form1.Designer.cs                   # Windows Forms auto-generated layout metadata
├── Form1.cs                            # UI Grid layout and event logic
├── Program.cs                          # Application entry point
└── ReservoirDesktopApp.csproj          # MSBuild project file targeting Windows Desktop SDK
